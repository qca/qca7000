diff --git a/qca-spi/qca_spi.c b/qca-spi/qca_spi.c
index 10c10c9..53e9bc9 100644
--- a/qca-spi/qca_spi.c
+++ b/qca-spi/qca_spi.c
@@ -47,6 +47,8 @@
 #include <linux/types.h>
 #include <linux/version.h>
 
+#include <mach/qca_bootstrap.h>
+
 /*====================================================================*
  *   custom header files;
  *--------------------------------------------------------------------*/
@@ -1022,6 +1024,9 @@ qcaspi_mod_init(void)
 	struct spi_device *spi_device = NULL;
 	struct qcaspi *qca = NULL;
 
+	/* reconfigure pins for SPI */
+	qca_set_host_mode(QCA_HOST_MODE_SPI);
+
 	printk(KERN_INFO "qcaspi: version %s\n", QCASPI_VERSION);
 
 	/* Validate module parameters. */
